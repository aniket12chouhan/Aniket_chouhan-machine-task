import React from 'react'

const Button = ({ title }) => {
    return (
        <button className='px-3 py-[6px] bg-[#E6BA95] text-white'>
            {title}
        </button>
    )
}

export default Button